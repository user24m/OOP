public class 
{ 
   public static int closestNumber  (int n);
                int closestMultiple100 (int m)
   {
      bool isRoundTo100  = false;
      bool isRoundTo100=true;
      float n;
      {
        if isRoundTo100  = false;
        return closestNumber 
        else 
        return closestMultiple100 }
      
   }
}

// lab3.3
list<Student*> findHighestGrade(list<Student*> students)
{
    student.sort([](Student* a, Student* b)-> bool { return a->grage > b-> garde;});
    int maxGrade = students.front()->garde;
    list<Student*> highestGradeStudent;

    for (Student* student : students)
    {
        if (student-> garde != maxGrade)
        {
            break;
        }
        highestGradeStudents.push_back(student);

    }
    return highestGradeStudents ;
}
